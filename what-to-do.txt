Radio bug — every track plays first track's audio [DONE — fixed in commit f29d8ba, all radio
  entry points now use playQueue with per-track streamUrlFn instead of play() constant closure]

UI too small — text, icons, everything: overall scale feels cramped. All text and icons should be
  at minimum 110% of current size, ideally larger. Do a global pass on font sizes, icon sizes,
  padding, and touch targets.

Context menu flipping: right-clicking near bottom of screen clips the menu instead of flipping it
  (menu should open upward when near viewport bottom). Applies to submenus too — submenus that would
  extend off-screen should flip to open leftward or upward as needed.
Radio mode indicator should show in queue panel (currently only visible elsewhere).
Radio mode popup closes too eagerly [DONE — switched to click-toggle + outside-click close]
Radio mode selector should also appear in the NowPlaying fullscreen view [DONE]
Queue sidebar: clicking a track should jump to it and start playing [DONE]
Queue sidebar: currently-playing track shows a play icon AND keeps its number, adding extra width.
  Play icon should replace the track number, not sit beside it [DONE]
Start radio from queue/Up Next: right-click a track in the queue → "Start Radio from this track" [DONE]
Queue should auto-close when entering the NowPlaying fullscreen view.

Fullscreen player bugs:
- Bottom bar (player controls) should not exist separately — fullscreen view must contain all controls itself
- Responsive layout breaks on height reduction: at some breakpoint, player shifts to top + tabs (up next/about/lyrics)
  below (good), but shrinking further causes lower section to overlay upper section. Volume, then controls, then
  progress bar get hidden under the overlapping content. Lower section must never overlap upper controls — either
  scroll the lower section, clamp its height, or use a flex layout that compresses gracefully.

NowPlaying right panel too narrow: fixed 400px right panel makes 2-column top tracks / suggested
  layouts completely unusable — columns are ~170px each. Need to rethink the split: either make
  right panel wider (500-600px), or use a different content layout (single column with more info,
  or a wider fluid approach). Left panel keeps the atmospheric art treatment; right panel needs
  enough room to actually display track grids legibly.
NowPlaying About tab — Top Tracks + Suggested don't fill panel width [DONE — grid-template-columns: repeat(2,1fr)]
NowPlaying About tab — Suggested track/album names overflow instead of truncating [DONE — truncation was already on track-title/track-album]
NowPlaying — clicking artist name navigates to Artists list view, not the specific artist page.
NowPlaying — album title not clickable; should navigate to that album's detail view [DONE]
NowPlaying — heart/love button should sit next to the volume control in the bottom bar, not buried
  in the main panel [DONE — heart added to PlayerBar right section]
NowPlaying About tab — clicking an album in "More From [Artist]" navigates to the album grid view
  (filtered by that artist?) instead of the specific album's detail page.
NowPlaying view redesign: current layout looks bad — needs a proper visual design pass.
  Ideas: blurred art background (done properly this time), better typography hierarchy,
  accent color extraction from art, smoother layout proportions, less "plain dark panel" feel.
Spacebar -> Pause [DONE]
NowPlaying view layout broken: everything squished to left side, content cramped together, rest of screen
  is just blurred album art — layout/grid/sizing is wrong, needs urgent fix [DONE but broken]
Album cover (small) -> click -> show album art enlarged as popover in bottom-left corner (above track/artist
  info in the player bar), NOT fullscreen — rest of player stays usable [in progress, old lightbox removed]
Queue -> album art not showing for tracks in the queue panel [already implemented per code review — verify in app]

--- Genre Tag / Filter Bugs ---
- Album view: Static Age (Misfits) shows genre "Hardcore [Punk]" on the album card, but clicking
  that genre chip filters to 2 albums — Static Age is NOT among them. Either the genre stored in
  SQLite doesn't match the chip label exactly (e.g. display normalizes brackets but filter queries
  raw value), or the album's genre tag wasn't written/read correctly and the chip is showing a
  derived/display value that doesn't round-trip to the filter query. Investigate tag read path +
  how genre chip onClick constructs the filter vs what's in SQLite.

--- Navigation / Scroll ---
- Scroll position should be preserved when navigating away and back [DONE — module-level scrollMemory
  Map in AlbumGrid + ArtistGrid, keyed by scrollKey prop from App]
- Album grid sorted by A-Z / Artist / Year should show a sticky alpha/index scrubber on the right
  edge (like iOS contacts sidebar) so users can jump to a letter or decade directly [DONE — alpha/decade
  scrubber in AlbumGrid, visible for alphabetical/artist/year sorts]

--- Empty State Copy ---
- "No albums yet. Syncing…" shows when filter returns zero results (e.g. filtering by Loved with no loved albums).
  "Syncing…" is wrong — there is no sync happening, the list is just empty. Fix copy to be context-aware:
  show "No loved albums" (or equivalent) when a filter is active and returns nothing, not a sync message.
- Loved filter toggle bug: click Loved (filters to empty), click again to deactivate — view breaks completely.
  Albums render as giant stacked full-width images instead of grid. Layout state corrupted on filter deactivate.

--- Tag Mapping Bugs ---
- "Rap" mapped to "Hip Hop" but still shows up on tracks (e.g. Take Care by Drake has Pop, Hip Hop, Rap).
  Mapping should suppress/replace the raw tag with the canonical one — investigate why raw tag still renders
- Manual genre/descriptor/scenes+movements editor: view and edit existing canonical tag entries, lock
  individual entries to prevent Last.fm troll tags from polluting the vocabulary
  (e.g. nonsensical tags that got pulled in and mapped incorrectly)

--- Artist Page Overhaul ---
- Tracks: clicking a track row does nothing — wire onClick to playQueue (same pattern as AlbumDetail)
  extend TopTrack query to include album_id + artwork_url; build CurrentTrack; add streamUrlFor
- Track list: 2-3 column grid instead of single long list
- Pull top tracks from Last.fm (not just local library tracks)
- Top tracks ranking: what is it based on? Currently likely play count from local library,
  which skews toward recently-added or heavily-ripped albums (e.g. Donda Chant as a top Ye track).
  Should rank by Last.fm global play count or user scrobble count — investigate current source
  and replace with something that reflects actual track popularity/significance
- Artist "correction" / merging: if two artist entries overlap (e.g. "The Beatles" vs "Beatles"),
  allow user to merge/alias them so they resolve to one canonical artist
  (think: how does this interact with pending_edits and tag writes?)

--- Radio Modes ---
Current radio = genre tree (60%) + Last.fm similar artists (25% boost). Add selectable modes:
- Same Genre: genre tree only, no Last.fm artist boost
- Same Artist: tracks by the seed track's artist only, no scoring
- Random: no weighting, pure random from library (exclude recently played)
- More ideas to consider:
    Similar Artists Only: Last.fm similar artists only, no genre tree
    Same Album: rest of the album, then similar
    Era/Decade: tracks from same decade as seed (need year tag)
    Loved Tracks: weighted toward loved/starred tracks

--- Tag Vocabulary View bugs ---
- Showing all mapped genres (trivial matches visible): scroll cuts off before reaching the end —
  fix with pagination sorted by first letter (A–Z sections or paginated alpha groups)
- Two section headers visible: "GENRES" and a separate blue "GENRE" — duplicate/stray label, investigate

--- Tag Clarity ---
- UX is confusing: which tags belong to a track vs album vs artist?
  e.g. is "genre" a track tag or album tag? is "year" on the track or the album?
  Need clear UI indication of tag ownership level (track / album / artist)
- Missing tags: what should the app show/do when a tag is absent at any level?
  (empty field? placeholder? warning? auto-inherit from album/artist?)

--- MusicBrainz Tagging ---
- Use MusicBrainz as additional metadata source alongside Last.fm
  e.g. official release date, label, catalog number, MBID, country, barcode
- MB data more authoritative for album-level facts; Last.fm better for popularity/tags
- Think: fetch on album open? background sync? only on manual "enrich" action?
- MBIDs anchor the album linking flow below

--- Album Identification / Right-Click Lookup Popup ---
- Right-click album → context menu → "Album Info / Identify..."
- Opens modal showing auto-detected match status for MusicBrainz + Last.fm:
    MusicBrainz: found ✓ / not found ✗ / ambiguous ? + key facts (title, artist, year, label)
    Last.fm: found ✓ / not found ✗ + key facts (play count, tags)
- Two MB ID levels matter:
    Release Group MBID = the abstract album (e.g. "Kind of Blue" regardless of edition) — primary anchor
    Release MBID = specific pressing (catalog #, barcode, country) — optional, for exact tag writes
- Last.fm has no stable album ID — it matches by artist name + album name strings.
  "Correction" for Last.fm = fix the name strings used to query it, not paste an ID.
  (Last.fm also returns MBIDs in responses — so a confirmed MB Release Group MBID serves as Last.fm anchor too)
- Editable fields in popup: MB Release Group MBID, MB Release MBID (optional), MB Artist MBID
  + corrected artist name / album name strings for Last.fm fallback
- "Re-fetch with these IDs" action re-runs both lookups with the corrected values
- Confirmed identity persists to new SQLite table:
    album_identity(album_id, mb_release_group_id, mb_release_id, mb_artist_id, lastfm_match_confirmed)
  Overrides auto-lookup. Feeds tag-write flow via pending_edits.
- Same popup (or variant) should work on Artist page for artist MBID correction

--- Code Quality ---
- rawGenre IN filter fragile: useAlbums.ts:50 does exact-match WHERE t.genre IN (?) against the
  full stored genre string. Currently safe because unmapped chips pass the full raw_value (e.g.
  "Rock, Pop") which matches exactly. But if any layer ever tokenises the chip name before passing
  rawGenre, the filter silently returns 0 results. Fix: either store genres as individual tokens
  in SQLite, or use a LIKE/INSTR query instead of IN for the raw genre path.
- RADIO_MODES constant duplicated: identical 8-entry array defined in both RadioChip.tsx (line 6)
  and StartRadioSubmenu.tsx (line 4). Extract to a shared constant in store/player.ts (where
  RadioMode type lives) and import from both files.
