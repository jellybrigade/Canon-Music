- Clicking a genre tag on an album (opened from Home) navigates to Home instead of Library filtered by that genre

- For You cards (all except Jump Back In and On Repeat) don't pick fresh albums on refresh — they just shift the queue by one
  - e.g. refresh 1: [A1, A2, A3, A4] → refresh 2: [A2, A3, A4, A5] → refresh 3: [A3, A4, A5, A6]
  - Should pick 4 genuinely new albums each refresh, not advance a sliding window

- MusicBrainz should be primary source of truth for artist name used in Last.fm lookups
  - When artist is MB-identified, the MB canonical name should be stored as lastfm_artist_name in artist_identity (not just the local library name)
  - useEnrichArtist already prefers lastfm_artist_name over raw artist.name — but that field only gets set on manual identify, not MB auto-identify
  - Fix: when MB auto-identifies an artist (or user confirms via IdentifyDialog), populate lastfm_artist_name from the MB artist name if not already set by the user
  - Also: if user manually overrides lastfm_artist_name in the identify dialog, that should take priority over MB name for Last.fm calls

- Artist page overhaul (design + UX)
  - Current layout is functional but sparse and not visually distinctive
  - Goals: cleaner hierarchy, better use of the banner area, bio feels more integrated, similar artists look like a real section not a chip dump
  - Consider: artist image as a proper portrait (not just blurred bg), top tracks and albums more visually separated, stats row more prominent
  - Needs design work before implementation

- Clicking artist name in Album detail view should navigate to that artist's page
- Custom For You categories: let user create their own card via filter builder in the Customize popup
  - Needs design work: which filters to expose (genre? decade? play count range? loved? unplayed? artist?), which to skip (too granular / redundant with built-ins)
  - Needs design work: how filters combine (AND? tag-style multi-select?)
  - Needs design work: UX for adding — button in popup opens a builder, user names it, picks filters, saves as a new entry alongside built-ins
  - Custom categories stored in settings (extend for_you_categories JSON with a filters field on each entry; built-ins have no filters)
